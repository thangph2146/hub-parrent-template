# script-system/api (legacy)



**Template render / sync / verify** đã chuyển vào **`packages/api-server/deploy/`**:



- `packages/api-server/deploy/cli/` — CLI

- `packages/api-server/deploy/config/` — cấu hình

- `packages/api-server/deploy/nest/` — mirror OOP



Root alias: `pnpm api:render`, `pnpm api:sync-template`, `pnpm verify:api-template`.



Còn lại ở đây: registry legacy, unified specs, merge tooling (`api-module-registry.cjs`, `generate-unified-*`, …).

