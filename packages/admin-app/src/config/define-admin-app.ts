import type { AdminAppConfig } from "./types"

/** Khai báo cấu hình admin app — dùng trong `admin.app.config.ts` của từng Next app. */
export function defineAdminApp(config: AdminAppConfig): AdminAppConfig {
  return config
}
