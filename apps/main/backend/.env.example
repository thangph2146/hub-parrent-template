# ENV_TEMPLATE=next-admin
# ENV_STACK=main,parent
# ==============================================================================
# Admin (@backend) — apps/main/backend
# ==============================================================================
# Copy:  cp .env.example .env
# Doc:   docs/env/README.md
# Dev:   pnpm --filter @backend dev
# Stack parent: API :3002 + app này :3001 + @frontend :3000
# ==============================================================================

# ------------------------------------------------------------------------------
# Kết nối API (bắt buộc)
# ------------------------------------------------------------------------------

NEXT_PUBLIC_API_URL=http://localhost:3002/api
NEXT_PUBLIC_API_PROXY=true
INTERNAL_API_URL=http://127.0.0.1:3002/api

# ------------------------------------------------------------------------------
# Runtime
# ------------------------------------------------------------------------------

NODE_ENV=development
# PORT=3001

# ------------------------------------------------------------------------------
# Routing (set TRƯỚC khi pnpm build)
# ------------------------------------------------------------------------------

NEXT_PUBLIC_BACKEND_BASE_PATH=
NEXT_PUBLIC_TRAILING_SLASH=false

# ------------------------------------------------------------------------------
# Bảo vệ admin (khớp PROTECTED_ADMIN_EMAILS trên API)
# ------------------------------------------------------------------------------

NEXT_PUBLIC_PROTECTED_ADMIN_EMAILS=superadmin@hub.edu.vn

# ------------------------------------------------------------------------------
# Tùy chọn
# ------------------------------------------------------------------------------

# NEXT_PUBLIC_ADMIN_URL=http://localhost:3001

# Không dùng DATABASE_URL / JWT — xem apps/main/api/.env
