/** CRUD runtime — template local (pnpm api:sync-template). */
/** Alias date helpers cho BaseCrudService vend. */
export { safeIsoString as toIso, safeIsoStringNow as toIsoNow } from '../date-utils';
