/** CRUD runtime — template local (pnpm api:sync-template). */
/** Re-export — single source: src/common/module-types/. */
export * from '../module-types/crud.types';
export type { PaginatedResult } from '../module-types/user.types';
