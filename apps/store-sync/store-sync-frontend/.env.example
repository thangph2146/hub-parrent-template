# ENV_TEMPLATE=next-storefront-store
# ENV_STACK=store
# ==============================================================================
# Storefront (@store-sync-frontend) — apps/store-sync/store-sync-frontend
# ==============================================================================
# Copy:  cp .env.example .env
# Doc:   docs/env/README.md
# Dev:   pnpm --filter @store-sync-frontend dev
# Stack store: @store-sync/api :3002 + app này :3000
# ==============================================================================

# ------------------------------------------------------------------------------
# Kết nối API (bắt buộc)
# ------------------------------------------------------------------------------

NEXT_PUBLIC_API_URL=http://localhost:3002/api

# ------------------------------------------------------------------------------
# Runtime
# ------------------------------------------------------------------------------

NODE_ENV=development
# PORT=3000

# ------------------------------------------------------------------------------
# Tùy chọn — maps / hỗ trợ
# ------------------------------------------------------------------------------

# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-maps-key
# NEXT_PUBLIC_ZALO_OA_URL=https://zalo.me/...

# Không dùng DATABASE_URL / JWT — xem apps/store-sync/api/.env
